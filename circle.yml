machine:
  ruby:
    version: 2.1.0
test:
  override:
    - bundle exec rake
deployment:
  staging:
    branch: master
    commands:
      - ./deploy/env.rb staging
  development:
    branch: master
    commands:
      - ./deploy/env.rb development
